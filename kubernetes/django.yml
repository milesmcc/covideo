apiVersion: "apps/v1"
kind: "Deployment"
metadata:
  name: "covideo-webserver"
  namespace: "default"
  labels:
    app: "covideo-django"
spec:
  replicas: 3
  selector:
    matchLabels:
      app: "covideo-django"
  template:
    metadata:
      labels:
        app: "covideo-django"
    spec:
      containers:
      - name: "covideo-django"
        image: "gcr.io/covideo/covideo:eb1cc9276bb1a94e54d27d4db8247deeef423646"
        command: ["./webserver.sh"]
        envFrom:
        - secretRef:
            name: django-settings
---
apiVersion: "apps/v1"
kind: "Deployment"
metadata:
  name: "covideo-celeryworker"
  namespace: "default"
  labels:
    app: "covideo-django"
spec:
  replicas: 3
  selector:
    matchLabels:
      app: "covideo-django"
  template:
    metadata:
      labels:
        app: "covideo-django"
    spec:
      containers:
      - name: "covideo-django"
        image: "gcr.io/covideo/covideo:eb1cc9276bb1a94e54d27d4db8247deeef423646"
        command: ["./celeryworker.sh"]
        envFrom:
        - secretRef:
            name: django-settings